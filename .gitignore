# Project specific
# Ignore all training checkpoints except the best one if needed, 
# but for now we trust the user's previous preference (which was tracking them).
# Common large files or artifacts
*.DS_Store
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# Mobile
node_modules/
.expo/
npm-debug.log
yarn-debug.log
yarn-error.log

# Experiments (Optional: ignore intermediate weights if too large)
# experiments/**/weights/last.pt

